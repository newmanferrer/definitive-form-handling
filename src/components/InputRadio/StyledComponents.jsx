//* ===========================================================================
//* 1.- Imports
//* ===========================================================================
import styled from 'styled-components';
import { COLORS } from '../../colors';
//* ===========================================================================

//* ===========================================================================
//* 2.- Reusable Components
//* ===========================================================================
//* 2.1.-
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* ===========================================================================

//* ===========================================================================
//* 3.- Used components
//* ===========================================================================
//* ---------------------------------------------------------------------------
//* 3.1.- Input Styled
//* ---------------------------------------------------------------------------
const InputStyled = styled.input`
 appearance: none;
 visibility: hidden;
 position: absolute;
 left: 0;
`;
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* 3.2.- Circle Image
//* ---------------------------------------------------------------------------
const CircleImage = styled.i`
 height: 16px;
 width: 16px;

 position: absolute;
 top: 4px;
 right: 0;

 transition: all 0.3s ease-in-out;

 border-radius: 50%;
 border: 2px solid ${COLORS.borderRadChe};
 background: ${COLORS.white};

 &::before {
  height: 10px;
  width: 10px;

  content: '';
  display: block;

  position: absolute;
  top: 1px;
  left: 1px;

  transition: all 0.3s ease-in-out;
  transform: scale(0);
  opacity: 0;

  background: ${COLORS.hover};
  border-radius: 50%;
 }
`;
//* ---------------------------------------------------------------------------
//* ---------------------------------------------------------------------------
//* 3.3.- Label And Input Wrapper
//* ---------------------------------------------------------------------------
const LabelAndInputWrapper = styled.label`
 padding-right: 25px;
 position: relative;
 font-family: serif;
 font-size: 1.2rem;

 &:hover {
  cursor: pointer;
 }

 ${InputStyled}:checked + ${CircleImage}::before {
  transform: scale(1);
  opacity: 1;
 }

 &:hover ${InputStyled}:not(:checked) + ${CircleImage} {
  background-color: ${COLORS.backgroundRadChe};
 }

 @media screen and (max-width: 280px) {
  width: 60%;
  display: grid;
 }
`;
//* ---------------------------------------------------------------------------

//* ===========================================================================

//* ===========================================================================
//* 4.- Exported components
//* ===========================================================================
export { LabelAndInputWrapper, InputStyled, CircleImage };
//* ===========================================================================
